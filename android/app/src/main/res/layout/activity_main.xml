<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🗺️ Localizador Familiar"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#1f2937"
        android:layout_marginBottom="20dp" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⏳ Aguardando..."
        android:textSize="16sp"
        android:textColor="#6b7280"
        android:layout_marginBottom="20dp"
        android:padding="16dp"
        android:background="#f3f4f6"
        android:gravity="center" />

    <Button
        android:id="@+id/startBtn"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="▶️ Iniciar Monitoramento"
        android:textSize="16sp"
        android:textColor="#fff"
        android:backgroundTint="#10b981"
        android:layout_marginBottom="12dp" />

    <Button
        android:id="@+id/emergencyTestBtn"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="🚨 Segure no botão flutuante por 3s"
        android:textSize="14sp"
        android:textColor="#fff"
        android:backgroundTint="#dc2626" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="O botão flutuante (🚨) permanecerá na tela. Segure-o por 3 segundos para ativar emergência. O app enviará localização a cada 10 minutos automaticamente (mínimo de bateria)."
        android:textSize="13sp"
        android:textColor="#6b7280"
        android:layout_marginTop="30dp"
        android:lineSpacingMultiplier="1.5" />

</LinearLayout>
